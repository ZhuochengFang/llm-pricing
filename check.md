# 实时检查项目是否正常运行
- 按照项目描述。llm价格表在每日中国时间9:00-22:00间每隔1个小时定时刷新，请通过读取日志refresh.log来确定项目是否在正常刷新，如果发现有异常缺失则尝试解决。

# 项目改进方向-20260727
注意到页面上模型列表的顺序比较乱，能否将其排序整理一下，按照以下原则：
- 在model_matcher里面，在两个平台上被认为是一个模型的模型放在一起，前面一个是openrouter后面一个是云雾AI，紧挨在一起。
- 相同模型、不同版本的模型放在一起，例如把claude-opus-4.6到claude-opus-5放在一起，按版本号从小到大排列，同时不违反第一条。
